# Write your MySQL query statement below
SELECT product_id,
Year as first_year,quantity,price
FROM Sales
Where (product_id,year) IN (
    SELECT product_id ,
    MIN(year)
    FROM Sales
    GROUP BY product_id
);
